{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lolo.serviceAccountName" . }}
  labels:
    {{- include "lolo.labels" . | nindent 4 }}
{{- end }}
